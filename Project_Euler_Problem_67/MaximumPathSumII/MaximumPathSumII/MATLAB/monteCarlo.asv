function [ Bm_ ] = monteCarlo( N_ )

    

    while( IMAX < 0 || IMAX > 2^53 )
    
        for i = 1:1:2
    
            IMAX = randi([0,OPT])*randi([0,2^53)])
    
        end
    end

    Km_ = randi([0,2^53]);

    Bm_ = permn( [ 0; 1 ], N_-1, Km_ ) );

    end

end